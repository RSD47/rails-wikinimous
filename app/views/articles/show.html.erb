<div class="ps-3  d-flex align-items-center justify-content-between">
  <h1><%= @article.title %></h1>
  <div class="me-3">
    <%= link_to("", edit_article_path, class: "fa-solid fa-pen text-decoration-none pe-3") %>
    <%= link_to("", article_path(@article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "fa-solid fa-trash text-decoration-none") %>
  </div>
</div>
<p class="ps-3"><%= @article.content %></p>
